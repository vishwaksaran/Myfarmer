'use client';

import ListingsBoard from '@/components/listings/ListingsBoard';

/** Buy & Sell — everything listed for sale, across all categories. */
export default function BuySellPage() {
    return <ListingsBoard mode="sale" />;
}
